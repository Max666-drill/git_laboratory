# 🖥️ Лабораторная работа №3: Система контроля версий Git

### Университет: ИТМО

## 🎯 Описание работы

Данная работа была посвящена освоению и практическому применению ключевых концепций системы контроля версий **Git**. В рамках симуляции работы над проектом были выполнены задачи по управлению историей коммитов, работе с ветками и разрешению конфликтов слияния.

Задания были сгенерированы автоматически для варианта **`504881`**.

## 🛠️ Продемонстрированные навыки

В ходе выполнения лабораторной работы были освоены следующие команды и механики Git:

*   **Базовые операции:**
    *   `git init`: Инициализация репозитория.
    *   `git add`: Добавление файлов в индекс.
    *   `git commit`: Создание коммитов с осмысленными сообщениями.

*   **Работа с ветками:**
    *   `git branch`: Управление ветками.
    *   `git checkout`: Переключение между ветками и коммитами.
    *   `git checkout -b`: Создание новой ветки с одновременным переключением.

*   **Анализ истории и изменений:**
    *   `git log`: Просмотр истории коммитов.
    *   `git diff`: Анализ различий между коммитами.

*   **Сложные операции и разрешение конфликтов:**
    *   `git merge`: Слияние веток и разрешение **конфликтов слияния** (`merge conflict`).
    *   `git rebase`: Перебазирование веток и разрешение **конфликтов во время `rebase`**.

---

## 🌳 Финальное дерево коммитов

Финальная история коммитов, сохраненная из репозитория после выполнения всех заданий. Она наглядно демонстрирует все слияния и ветвления.

```bash
*   b76f386 Merge branch 'feature2'
|\
| * 3f93363 feature2 change
* |   cb91fd4 Merge branch 'feature1'
|\ \
| * | 6cc7600 feature1 change
| |/
* | 75ee47b добавлены не алгосы
* | dc75140 добавлены алгосы
|/
* 6cd31dd add todo.txt 5
* 1cde9b0 add todo.txt 4
* 4eb0196 add todo.txt 3
* 224c0d9 add todo.txt 2
* fabc128 add todo.txt 1
* a67489b init
```

---

## 📂 Структура репозитория

*   `history.txt` - Полная история коммитов в виде дерева (результат команды `git log --oneline --graph --all`).
*   `diff.txt` - Пример вывода команды `git diff`, показывающий разницу между двумя коммитами.
*   Остальные файлы (`todo.txt`, `init.txt` и т.д.) - рабочие файлы, использовавшиеся в ходе выполнения заданий.

